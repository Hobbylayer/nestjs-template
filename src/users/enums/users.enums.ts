export enum StatusUser {
    ACTIVE = "active",
    DISABLED = "disabled",
    PENDING_ACTIVATION = "pending-activation",
}

export enum Roles {
    SUPER_ADMIN = "super-admin",
    ADMIN = "admin",
    RESIDENT = "resident",
}